package SkillCheck;

import java.time.LocalTime;
import java.time.temporal.ChronoUnit;

public class solution3 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		String m = "ABCDEFG";
		String[] musicinfos = { "12:00", "12:14", "HELLO", "CDEFGAB", "13:00", "13:05", "WORLD", "ABCDEF" };
		String answer = "";

		for (int i = 0; i < musicinfos.length; i += 4) {

			LocalTime start = LocalTime.parse(musicinfos[i]);
			LocalTime finish = LocalTime.parse(musicinfos[i + 1]);

			int play = (int) start.until(finish, ChronoUnit.MINUTES);
			
			//재생시간 < 코드길이
			if(play<musicinfos[i+3].length())
			//재생시간 > 코드길이
			
			
			
			
		}

	}

}
