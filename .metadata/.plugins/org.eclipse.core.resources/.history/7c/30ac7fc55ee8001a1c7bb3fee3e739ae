import java.util.LinkedList;
import java.util.Queue;
import java.util.Scanner;

public class bj_2468 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Scanner sc = new Scanner(System.in);

		int N = sc.nextInt();

		int area[][] = new int[N][N];
		int max = 0;
		for (int i = 0; i < N; i++) {
			for (int j = 0; j < N; j++) {
				area[i][j] = sc.nextInt();
				max = (max < area[i][j]) ? area[i][j] : max;
			}
		}

		for (int height = 1; height < max; height++) { // 물에 잠길 수 있는 최대 높이 =max
			boolean sink[][] = new boolean[N][N];

			for (int i = 0; i < N; i++) {
				for (int j = 0; j < N; j++) {
					if (!sink[i][j] && area[i][j] <= height) {
						Queue<Node> qu = new LinkedList<Node>();
					}
				}
			}
		}

	}

	static class Node {
		int y;
		int x;

		Node(int y, int x) {
			this.y = y;
			this.x = x;
		}
	}

}
